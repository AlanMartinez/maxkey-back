change: admin-dashboard
project: maxkeys
artifact_store: hybrid
execution_mode: auto
delivery_strategy: ask-on-risk
review_budget_lines: 800
created: 2026-09-14
updated: 2026-09-14
phases:
  explore:
    status: done
    artifact: exploration.md
    engram_topic: sdd/admin-dashboard/explore
    engram_id: 294
  proposal:
    status: done
    artifact: proposal.md
    engram_topic: sdd/admin-dashboard/proposal
    slices_forecast: 5 (back: catalog / carousel / buyers+resend; front: guard+catalog+carousel / buyers)
  spec:
    status: done
    artifact: specs/{admin-catalog,carousel,admin-buyers,fulfillment}/spec.md
    engram_topic: sdd/admin-dashboard/spec
    note: admin-catalog, carousel, admin-buyers are new full specs; fulfillment is a MODIFIED delta on "One-Time Delivery Email" (admin resend)
  design:
    status: done
    artifact: design.md
    engram_topic: sdd/admin-dashboard/design
    engram_id: 298
    note: D1 new outbox type OrderDeliveryResendRequested (adminSub in payload); D3 PUT endpoints, no toggle method; D4 /admin/buyers substring email search, page<=100; D5 GET /admin/me for middleware/admin.ts
  tasks:
    status: done
    artifact: tasks.md
    engram_topic: sdd/admin-dashboard/tasks
    note: 5 phases = 5 stacked PRs (chain strategy already fixed by design D7); est. lines 400/600/450/550/250 (total ~2250), 800-line budget risk Low; decision needed before apply No
  apply:
    status: done
    engram_topic: sdd/admin-dashboard/apply-progress
    note: slice 1 (Phase 1, back PR1 admin catalog), slice 2 (Phase 2, back PR2 carousel), and slice 3 (Phase 3, back PR3 buyers+resend) all complete on branch feat/admin-buyers (from feat/carousel-slides, from feat/admin-catalog) — 15/15 Phase 3 tasks done; dotnet test 223/223 passed (was 207); not pushed, no PR opened; slice 3 authored diff (900 lines, 315 production + 585 tests, 0 generated) exceeds the 800-line hard cap — needs size:exception decision before PR3 opens, same pattern as slice 2. Slice 4 (Phase 4, front PR4 guard+catalog+carousel) complete on maxkeys-front branch feat/admin-area (from origin/main) — 9/9 Phase 4 tasks done plus 1 coordinator-requested correction commit; npm test 62/62 passed, nuxi typecheck clean, nuxt build succeeded; authored diff vs origin/main is 701 lines (677 insertions + 24 deletions) across 4 commits, under the 800-line cap; not pushed, no PR opened. Remaining deviation from the literal task text, explicitly directed by the apply prompt: admin/{ProductEditor,VariantRow,SlideForm}.vue are presentational and emit events instead of each calling useApi() directly — the two new composables useAdminCatalog/useAdminCarousel own the API calls. The HeroCarousel.vue fallback deviation was resolved: the first apply pass removed the static fallback (per an earlier reading of the apply prompt's scope text); a coordinator follow-up then required restoring design D2's Data Flow behavior (static FALLBACK_SLIDES on empty response OR fetch error, since the frontend may deploy before backend PR #29 is live) — committed separately as fix(carousel): keep static fallback slides when the API is empty or fails. Final HeroCarousel.vue behavior now matches design.md exactly. Also confirmed (per the same follow-up) that no placeholder-style image asset exists anywhere under public/ to reuse for utils/productImage.ts's PLACEHOLDER_IMAGE — it still points at a not-yet-created public/images/products/placeholder.png, a real pre-existing gap for whoever owns static assets. Slice 5 (Phase 5, front PR5 buyers+resend) now complete on maxkeys-front branch feat/admin-buyers-ui (from feat/admin-area) — 4/4 Phase 5 tasks done in 1 commit (0497596); npm test 67/67 passed (62 baseline + 5 new), nuxi typecheck clean, nuxt build succeeded; authored diff vs feat/admin-area is 394 lines (391 insertions + 3 deletions), over the ~250 forecast but well under the 800-line hard cap — no size:exception needed; not pushed, no PR opened. Deviation: test files are tests/useAdminBuyers.spec.ts + tests/adminBuyersPage.spec.ts (repo convention keeps specs under tests/, not colocated under pages/ as the task text literally says) — functionally equivalent coverage. All 5 slices now applied across both repos (49/49 tasks total per tasks.md); ready for slice 5 sdd-verify.
  verify:
    status: done
    artifact: verify-report.md
    engram_topic: sdd/admin-dashboard/verify-report
    note: ALL 5 SLICES VERIFIED, FINAL. Slice 1 PASS (remediated, 174/174 tests). Slice 2 PASS (0 CRITICAL/0 WARNING/2 SUGGESTION, 207/207 tests). Slice 3 PASS (WARNING resolved and confirmed this pass -- adminSub outbox payload now uses JsonSerializer.Serialize per commit d522915; 0 CRITICAL/0 WARNING/3 SUGGESTION, 223/223 tests, 15/15 Phase 3 tasks). Slice 4 (front PR4 guard+catalog+carousel) PASS (0 CRITICAL/0 WARNING/3 SUGGESTION, 62/62 frontend tests, typecheck clean, build green, 9/9 Phase 4 tasks). Slice 5 (front PR5 buyers, maxkeys-front, branch feat/admin-buyers-ui) PASS (0 CRITICAL/0 WARNING/3 SUGGESTION, 67/67 frontend tests, typecheck clean, build green, 4/4 Phase 5 tasks; contract-match cross-check against backend BuyersDtos/AdminBuyersEndpoints/AdminEndpoints found zero mismatches). Combined across all 5 slices: backend 223/223 + frontend 67/67 = 290/290 tests passing, 0 CRITICAL, 0 WARNING, 12 SUGGESTION (none blocking; the recurring untracked backend Properties/ hygiene item is one open item counted once per slice section, not 5 separate issues). Deploy order per design D7: backend #1 then #2 then #3 before frontend PR5 (buyers) is exposed to real admin traffic; frontend PR4 (catalog/carousel) is safe to deploy once backend #1+#2 are live thanks to the HeroCarousel static fallback and the fail-closed admin guard.
  archive:
    status: pending
repositories:
  backend: https://github.com/AlanMartinez/maxkey-back.git (this repo; owns openspec/)
  frontend: https://github.com/AlanMartinez/maxkey-front.git (Nuxt 3; slices 4-5)
next_recommended: sdd-archive for all 5 slices (change complete, all verified PASS, 0 CRITICAL/0 WARNING); optionally resolve the recurring backend Properties/ hygiene SUGGESTION before opening PR1.
decisions:
  - images v1 = paste existing R2 ImageKey, ADR-12 intact (Engram sdd/admin-dashboard/decisions #295)
  - buyers = read-only view + resend delivery email; no revoke/reissue/ban/roles
  - carousel slides reference ProductId; image/link derive from product unless overridden
  - admin auth = existing AdminPolicy / Auth:AdminSubs allowlist
  - admin UI = protected area in maxkeys-front
open_items:
  - follow-up: make --seed-catalog insert-only once admin editing exists (design Open Questions)
  - follow-up: resend rate limit if abused (design Open Questions)
