change: admin-dashboard
project: maxkeys
artifact_store: hybrid
execution_mode: auto
delivery_strategy: ask-on-risk
review_budget_lines: 800
created: 2026-09-14
updated: 2026-09-14
phases:
  explore:
    status: done
    artifact: exploration.md
    engram_topic: sdd/admin-dashboard/explore
    engram_id: 294
  proposal:
    status: done
    artifact: proposal.md
    engram_topic: sdd/admin-dashboard/proposal
    slices_forecast: 5 (back: catalog / carousel / buyers+resend; front: guard+catalog+carousel / buyers)
  spec:
    status: done
    artifact: specs/{admin-catalog,carousel,admin-buyers,fulfillment}/spec.md
    engram_topic: sdd/admin-dashboard/spec
    note: admin-catalog, carousel, admin-buyers are new full specs; fulfillment is a MODIFIED delta on "One-Time Delivery Email" (admin resend)
  design:
    status: done
    artifact: design.md
    engram_topic: sdd/admin-dashboard/design
    engram_id: 298
    note: D1 new outbox type OrderDeliveryResendRequested (adminSub in payload); D3 PUT endpoints, no toggle method; D4 /admin/buyers substring email search, page<=100; D5 GET /admin/me for middleware/admin.ts
  tasks:
    status: done
    artifact: tasks.md
    engram_topic: sdd/admin-dashboard/tasks
    note: 5 phases = 5 stacked PRs (chain strategy already fixed by design D7); est. lines 400/600/450/550/250 (total ~2250), 800-line budget risk Low; decision needed before apply No
  apply:
    status: in_progress
    engram_topic: sdd/admin-dashboard/apply-progress
    note: slice 1 (Phase 1, back PR1 admin catalog), slice 2 (Phase 2, back PR2 carousel), and slice 3 (Phase 3, back PR3 buyers+resend) all complete on branch feat/admin-buyers (from feat/carousel-slides, from feat/admin-catalog) — 15/15 Phase 3 tasks done; dotnet test 223/223 passed (was 207); not pushed, no PR opened; slice 3 authored diff (900 lines, 315 production + 585 tests, 0 generated) exceeds the 800-line hard cap — needs size:exception decision before PR3 opens, same pattern as slice 2; slices 4-5 not started (live in maxkeys-front, blocked on backend deploy)
  verify:
    status: partial
    artifact: verify-report.md
    engram_topic: sdd/admin-dashboard/verify-report
    note: slices 1-3 (back PR1 admin catalog, PR2 carousel, PR3 buyers+resend) all verified. Slice 1 PASS (remediated, 174/174 tests). Slice 2 PASS (0 CRITICAL/0 WARNING/2 SUGGESTION, 207/207 tests). Slice 3 PASS WITH WARNINGS (0 CRITICAL/1 WARNING/3 SUGGESTION, 223/223 tests, 15/15 Phase 3 tasks, all 7 admin-buyers + 3 fulfillment-delta scenarios covered with runtime evidence; WARNING = unescaped adminSub string interpolation into the OrderDeliveryResendRequested outbox JSON payload, not exploitable today given the closed AdminPolicy allowlist but a real robustness gap; size:exception honored for the accepted PR2/PR3 budget overages). Slices 4-5 (maxkeys-front) not applied, not verified, blocked on backend deploy per design D7.
  archive:
    status: pending
repositories:
  backend: https://github.com/AlanMartinez/maxkey-back.git (this repo; owns openspec/)
  frontend: https://github.com/AlanMartinez/maxkey-front.git (Nuxt 3; slices 4-5)
next_recommended: sdd-archive for slices 1-3 (backend, all verified) once the slice-3 WARNING (adminSub JSON escaping in RequestDeliveryResend) is triaged/accepted by the maintainer; slices 4-5 (maxkeys-front) remain blocked on backend deploy per design D7 and are not yet ready for sdd-apply.
decisions:
  - images v1 = paste existing R2 ImageKey, ADR-12 intact (Engram sdd/admin-dashboard/decisions #295)
  - buyers = read-only view + resend delivery email; no revoke/reissue/ban/roles
  - carousel slides reference ProductId; image/link derive from product unless overridden
  - admin auth = existing AdminPolicy / Auth:AdminSubs allowlist
  - admin UI = protected area in maxkeys-front
open_items:
  - follow-up: make --seed-catalog insert-only once admin editing exists (design Open Questions)
  - follow-up: resend rate limit if abused (design Open Questions)
