change: mvp-marketplace
project: maxkeys
artifact_store: hybrid
execution_mode: interactive
delivery_strategy: auto-chain
review_budget_lines: 400
created: 2026-09-12
updated: 2026-09-12
phases:
  explore:
    status: done
    artifact: exploration.md
    engram_topic: sdd/mvp-marketplace/explore
  proposal:
    status: done
    artifact: proposal.md
    engram_topic: sdd/mvp-marketplace/proposal
  spec:
    status: done
    artifact: specs/{catalog,cart-checkout,payments-webhook,outbox-processing,fulfillment,auth,orders-history}/spec.md
    engram_topic: sdd/mvp-marketplace/spec/{domain}
  design:
    status: done
    artifact: design.md
    engram_topic: sdd/mvp-marketplace/design
    gate: fail-then-pass (1 corrective pass; fresh-context validator)
  tasks:
    status: done
    artifact: tasks.md
    engram_topic: sdd/mvp-marketplace/tasks
    prs: 19 (PR0 bootstrap + 18 design slices)
    workload_forecast: chained=yes, budget_risk=medium, decision_needed=no
    chain_strategy: stacked-to-main
  apply:
    status: in-progress
    engram_topic: sdd/mvp-marketplace/apply-progress
    completed_prs_backend: [PR0 main, PR1a #1 merged, PR1b #2, PR2a #3, catch-up #5 merged, PR2b #4, PR3a #6, PR3b #7, PR4 #8, PR5a #9, PR5a2 #10, catch-up #11 (lands PR2b-PR5a2, OPEN), PR5b #12, PR6 #13, PR7a #14, PR7b #15]
    completed_prs_frontend: [PR13 #1 merged, PR14 #2 merged, PR15 #3, PR16 #4, PR17a #5, PR17b #6, catch-up #7 (lands PR15-PR17b), PR18b #8, dev-server fix #9 (unverified smoke)]
    in_progress: none (session ended 2026-09-13; all agents stopped cleanly)
    next_backend: PR8 feat/mvp-08-fulfillment-application (base feat/mvp-07b-outbox-processor; tasks 8.1-8.7 incl. spec carry-over 8.6; est ~380 -> pre-split 8a AttachKey+ListAwaiting+tests / 8b OrderDeliveredHandler+email template+GetMyOrders+tests)
    next_frontend: none until backend API exists (PR9); then end-to-end smoke; verify dev-server pipe error after fix #9
    attempt_ledger: backend and frontend ledgers clean (last units settled complete); acquire with --max-changed-lines 900 per work unit
    merge_queue_backend: '#11 -> #12 -> #13 -> #14 -> #15 (all opened against main; merge in order, each diff collapses after the previous merge)'
    merge_queue_frontend: '#7 -> #8 -> #9 (against main)'
    follow_ups: [task 12.0 Product.Description domain gap, Supabase signing mode CONFIRMED asymmetric ECC P-256 (Auth:Mode=Jwks) - PR10 unblocked, Google provider still DISABLED in Supabase (user must add Google Cloud client id/secret + redirect URLs), MP x-signature sandbox check in PR11, Email:OperatorTo vs design Email:OperatorAddress naming reconcile in PR12, .gitattributes for LF, frontend dev-server pipe error smoke]
repositories:
  backend: https://github.com/AlanMartinez/maxkey-back.git (this repo; owns openspec/)
  frontend: https://github.com/AlanMartinez/maxkey-front.git (separate repo, PR13-PR17; layout amendment pending)
  verify:
    status: pending
  archive:
    status: pending
next_recommended: apply (start PR0 repo bootstrap)
tasks_carryovers:
  - add caps (<=20 items, quantity 1..10) to cart-checkout spec as 422 scenarios
  - clarify fulfillment 'One-Time Delivery Email' wording: one send per Delivered transition, at-least-once via OrderDelivered outbox event
  - design defines 18 PR slices (design.md section 13), all estimated under 400 lines; auto-chain applies
decisions:
  - cart: multi-item with OrderItem (Engram sdd/mvp-marketplace/decision-cart)
  - delivery: all-or-nothing (Engram sdd/mvp-marketplace/decision-delivery)
  - auth: JWKS primary, HS256 fallback by config (Engram sdd/mvp-marketplace/decision-auth-jwt)
open_items:
  - confirm Supabase JWT signing mode before applying the auth slice
  - email provider: generic SMTP via MailKit (design ADR-08), concrete provider chosen at deploy
  - proposal question round confirmed 2026-09-12 (refunds fully out of scope)
